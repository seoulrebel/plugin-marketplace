# xAI marketplace compatibility report

Generated by `scripts/convert_xai_catalog.py`.

## Retained

21 entries are published in `.agents/plugins/marketplace.json`.

Remote entries retain the original pinned commit SHA. Stripe and TinyFish use their verified non-Grok subtrees at the same pinned SHA. The seven Grok/Cursor-only sources are vendored at their original pinned SHAs and converted to `.codex-plugin/plugin.json`; Neon is also vendored locally.

## Vendored conversions

- `axiom` — local source under `external_plugins/axiom`.
- `browser-use` — local source under `external_plugins/browser-use`.
- `exa` — local source under `external_plugins/exa`.
- `firecrawl` — local source under `external_plugins/firecrawl`.
- `pstack` — local source under `external_plugins/pstack`.
- `sentry` — local source under `external_plugins/sentry`.
- `tavily` — local source under `external_plugins/tavily`.

The original xAI catalog remains preserved in `source_docs/xai-original/marketplace.json`.
